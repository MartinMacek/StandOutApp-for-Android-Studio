<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.martin.MyApplication">
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>
    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name" >
        <activity
            android:name="com.example.martin.MyApplication.StandOutExampleActivity"
            android:excludeFromRecents="true"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoDisplay" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name="com.example.martin.MyApplication.MultiWindow" >
        </service>
        <service android:name="com.example.martin.MyApplication.SimpleWindow" >
        </service>
        <service android:name="com.example.martin.MyApplication.MostBasicWindow" >
        </service>
        <service android:name="com.example.martin.MyApplication.WidgetsWindow" >
        </service>
    </application>

</manifest>